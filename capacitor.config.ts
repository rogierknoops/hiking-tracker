import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rogier.hikingtracker',
  appName: 'Hiking Tracker',
  webDir: 'dist'
};

export default config;
